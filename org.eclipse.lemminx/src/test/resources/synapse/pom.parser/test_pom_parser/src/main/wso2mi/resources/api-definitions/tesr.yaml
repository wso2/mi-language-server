openapi: 3.0.1
info:
  title: tesr
  description: API Definition of tesr
  version: 1.0.0
servers:
- url: http://localhost:8290/tesr
paths:
  /:
    get:
      responses:
        default:
          description: Default response
